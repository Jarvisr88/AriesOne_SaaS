from enum import Enum

class ApplicationName(Enum):
    DDE = "DDE"
    PPTN = "PPTN"
    CSI = "CSI"

# Documentation
# The ApplicationName enum defines a set of named constants representing different application names.
# It provides a type-safe way to work with application names in the modernized system.
